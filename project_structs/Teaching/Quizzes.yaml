# Generate quizzes to assess students.
- assessments:

  - $teaching-preamble.sty: teaching.sty

{mp:code=
quiz_string="""  - quiz_{:02d}:
    - $teaching-quiz.tex: quiz_{:02d}.tex"""
for i in range(1, 8):
  print(quiz_string.format(i, i))
/}