%=============================================================================%
%========= Imports
%=============================================================================%
\usepackage{geometry}
\geometry{
    top=0.8in, bottom=0.8in,
    left=0.5in, right=0.5in,
}

% Math symbols, like \mathbb{R}
\usepackage{amssymb}

% Common math environments, like bmatrix
\usepackage{amsmath}

% Argument parsing
\usepackage{listofitems}

% Multi column support
\usepackage{multicol}

% Colored text
\usepackage{xcolor}
\definecolor{jd_blue}{HTML}{6CA3D8}

% Tables
\usepackage{tabularx}

%======================== Final Imports ========================%
\usepackage{hyperref}
\hypersetup{
    colorlinks = false,
    linktoc = all,
    citecolor = jd_blue,
    citebordercolor = jd_blue,
    linkcolor = jd_blue,
    linkbordercolor = jd_blue,
    urlcolor = jd_blue,
    urlbordercolor = jd_blue,
}

%======================== Worksheet Functionality ========================%
\NewDocumentCommand{\includesection}{ m }{
    \setsepchar{-}
    % File name to problem sheet, provided as 1-1 to mean
    % chapter_1/1-1.tex, for problems on chapter 1 section 1
    \readlist\probsfname{#1}
    % Include section contents
    \input{../../problems/chapter_\probsfname[1]/\probsfname[1]-\probsfname[2]}
}


%=============================================================================%
%========= Formatting
%=============================================================================%

%======================== Headers ========================%
\let\examheader\header % update the header command
\RenewDocumentCommand{\header}{ m m m }{
    \firstpageheader
    {{\large\bfseries #1}}
    {{\bfseries #2}}
    {{\large\color{gray} #3}}
    \runningheader
    {#1}
    {}
    {}
}


\newcommand*{\subheader}[1]{
    \begin{center}
        \subsubsection*{\underline{#1}}
    \end{center}
}


%======================== Footers ========================%
\newcommand*{\printemailfooter}{
    \firstpagefooter{\url{jonathan.delgado@uci.edu}}{}{}
}

\runningfooter{}{}{Page \thepage}


%======================== Grading ========================%
\newcommand*{\printgradetable}{%
    \begin{center}%
        \gradetable%
    \end{center}%
}


%======================== Student information ========================%
\newcommand*{\printnamefield}{%
    {\bfseries Name: \hrulefill}%
}


%=============================================================================%
%========= Custom Commands
%=============================================================================%
\newcommand*{\R}{\mathbb{R}}
\DeclareMathOperator{\Mat}{Mat}